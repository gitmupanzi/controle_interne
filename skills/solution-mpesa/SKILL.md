---
name: solution-mpesa
description: Importer, normaliser, contrôler et rapprocher les fichiers Excel M-PESA de la Solution Numérique, des rapports G2 M-Pesa et de Perfect; construire les sous-onglets Importation et contrôle, Extraits clients, Finance et comptabilité, Épargnes, Crédits, Solution Numérique / M-Pesa, Perfect Client, Statistiques et Projections; produire la balance auxiliaire client, mesurer les flux, remboursements, nouveaux crédits, encours, PAR, épargne, DAT, concentration et risques, détecter les anomalies et produire les exports Excel ciblés, Word et PDF sans mélanger les devises. Utiliser pour toute question ou modification liée à Solution M-PESA, Bisou Bisou Digital, Solution Numérique/Portal/Turbo, Solution Numérique/G2, Perfect, Phone_Prefixe, Receipt No/ref_no, DAT, épargne, crédit, comptabilité, balance, fidélisation, rapprochement client ou rapport M-PESA du projet Streamlit.
---

# Solution M-PESA

Réutiliser les contrats et fonctions métier existants. Préserver la traçabilité des sources, le grain des opérations et la séparation stricte des devises.

## Procédure de travail

1. Identifier les fichiers disponibles, la période, le sens des flux et le livrable demandé.
2. Lire [references/data-contracts.md](references/data-contracts.md) pour sélectionner les colonnes, clés, classifications et règles de dégradation.
3. Exécuter `scripts/inspect_mpesa_contracts.py` avant de modifier un import ou un alias.
4. Normaliser avec les fonctions `prepare_*` de `credit_app/services/mpesa_analysis.py`; ne pas reproduire cette logique dans Streamlit.
5. Valider les colonnes obligatoires et afficher les colonnes manquantes ainsi que les colonnes réellement disponibles.
6. Construire les rapprochements et classifications dans le service, puis limiter l'interface au filtrage et au rendu.
7. Conserver les indicateurs de correspondance, motifs d'écart, lignes non rapprochées et sources facultatives absentes.
8. Calculer chaque solde, total et taux par devise. Ne jamais sommer CDF et USD.
9. Tester les cas nominal, source absente, format G2 alternatif, doublon, référence inconnue, contrôle en écart et solde d'ouverture manquant.

Tous les téléversements de Solution M-PESA peuvent recevoir plusieurs fichiers. Conserver leur provenance, supprimer les chevauchements avec la clé métier propre à chaque source et ne jamais additionner plusieurs instantanés du même compte, crédit ou client.

Dans les libellés destinés aux utilisateurs, utiliser `Solution Numérique`. Conserver `Turbo` seulement lorsqu'il s'agit d'un nom technique déjà contractuel : colonnes sources, fonctions, clés Streamlit, feuilles Excel historiques, tests, fichiers exportés ou libellés de traçabilité. `G2` doit être présenté comme `Solution Numérique/G2` ou `Rapport G2` lorsqu'il désigne le relevé de contrôle; il ne constitue pas un canal financier distinct de M-Pesa et ne doit jamais être additionné séparément aux montants M-Pesa. Réserver `Solution M-PESA` au nom global du module; ne jamais utiliser `M-PESA` seul comme source de calcul d'un indicateur. Exception : les exports officiels de l'Extrait client ne contiennent aucun suffixe `[Turbo]`, car ils constituent des relevés clients et non des écrans de traçabilité technique.

## Noms de colonnes visibles

- L'option globale du sidebar `Reference et stockage > Preparation des donnees > Renommer automatiquement les colonnes` pilote aussi Solution Numerique. Si elle est cochee, les tableaux visibles et les exports Excel doivent afficher des noms de colonnes propres, en francais, minuscules, sans accents, au format `snake_case` et conformes a `^[a-z0-9_]+$`.
- Le renommage utilisateur est applique uniquement a la frontiere d'affichage ou d'export. Les DataFrames metier internes conservent leurs colonnes techniques (`customer_id`, `currency_code`, `created_at`, `ref_no`, etc.) afin de ne pas casser les calculs, filtres, rapprochements et tests existants.
- `data/Rename_columns.xlsx` reste la source centrale des alias et des noms cibles. Avant d'ajouter un alias Solution Numerique, verifier le contrat avec `scripts/inspect_mpesa_contracts.py`, puis ajouter la correspondance dans ce fichier plutot que de multiplier les dictionnaires locaux.
- Les colonnes techniques de `Loans Account` et des analyses Credit doivent avoir une correspondance francaise dans `data/Rename_columns.xlsx`, notamment les champs de solde, encours, interets, frais, penalites, echeance, statut, telephone, compte epargne source et indicateurs de risque. Ne jamais exposer des libelles comme `outstanding_interest`, `interest_calculated`, `savings_account_id_source` ou `telephone_credit` dans un tableau utilisateur lorsque le renommage automatique est actif.
- Les collisions doivent etre resolues sans perte de donnees. Si deux colonnes sources aboutissent au meme nom visible, garder les deux colonnes et ajouter un suffixe stable au second nom.
- Quand l'option du sidebar est desactivee, les tableaux et exports conservent les noms techniques originaux. Le choix ne doit jamais modifier les fichiers Excel sources ni les DataFrames caches.

## Décision de refactoring des téléversements

État : refactoring Streamlit implémenté. Le parcours principal affiche quatre sources Solution Numérique et deux compléments facultatifs.

- Réduire le parcours principal Solution Numérique à quatre téléversements : `Transactions`, `Savings Account`, `Loans Account` et `Customers`.
- Traiter `Savings Account` comme l'unique téléversement principal pour l'épargne courante et les DAT. En déduire deux jeux internes : tous les `NORMAL SAVINGS` et tous les `FIXED SAVINGS`, soldes positifs ou nuls.
- Ne plus exiger deux téléversements distincts pour `Customers with Current Savings Account` et `Customers with Fixed Savings Account`. Ce sont des vues filtrées à solde positif de `Savings Account`, pas des sources exhaustives.
- Ne pas afficher deux widgets séparés pour les vues résumées. Le téléversement multiple unique `Savings Account` accepte soit la source complète recommandée, soit les deux fichiers `Customers with Current Savings Account` et `Customers with Fixed Savings Account` chargés ensemble en mode de compatibilité.
- Si la source complète et les vues résumées sont chargées ensemble, donner la priorité à la source complète et ignorer les lignes résumées afin d'éviter les doublons. Si seules les synthèses sont présentes, signaler que les comptes à solde nul et l'historique exhaustif sont indisponibles.
- Conserver un seul téléversement multiple G2 facultatif pour réunir les entrées 1441 et les sorties 15558, puis un téléversement `Clients_Perfect` facultatif.
- Interface en production : quatre emplacements Solution Numérique principaux, plus G2 et Perfect comme compléments facultatifs. L'emplacement `Savings Account` porte aussi le repli compatible Current + Fixed sans créer de nouveaux widgets. Ne pas dupliquer une donnée déjà démontrable depuis la source complète.
- Maintenir les tests de parité sur les comptes, soldes, devises, statuts et dates. Tous les sous-onglets doivent fonctionner avec les quatre sources Solution Numérique principales, sans fichiers résumés.

Cas réel de référence du 17 juillet 2026 : `Savings Account` contient 77 084 comptes courants, dont 862 à solde positif, et 3 707 DAT, dont 1 214 à solde positif. Le résumé Current Savings correspond exactement aux 862 comptes courants positifs; le résumé Fixed Savings correspond exactement aux 1 214 DAT positifs.

## Cockpit Épargnes

Référence détaillée : [references/kpi_epargne.md](references/kpi_epargne.md).

- L'onglet `Épargnes` est le cockpit unique de l'épargne courante et des DAT; ne pas créer d'onglet parallèle pour ces analyses.
- `Savings Account` est la source maître de stock actuel : comptes ouverts `NORMAL SAVINGS`, DAT `FIXED SAVINGS`, soldes, statuts, produits, dates contractuelles, intérêts constatés et frais disponibles.
- `Transactions [Solution Numérique]` est la source des flux de période : dépôts, retraits, transferts DAT, retours DAT et remboursements depuis compte ouvert. Ne jamais compter les lignes comptables brutes comme transactions clients; utiliser les événements canoniques.
- G2 reste facultatif et sert à l'identité ou au rapprochement; il ne calcule ni encours épargne, ni dépôts, ni retraits, ni DAT lorsque la source Solution Numérique est disponible.
- Toujours distinguer `stock_actuel` et `flux_periode`. Ne pas tracer une évolution historique de l'encours épargne ou DAT depuis un seul fichier `Savings Account`.
- Conserver les comptes à solde nul dans le portefeuille actuel lorsque la source complète `Savings Account` est chargée; les vues résumées Current/Fixed ne couvrent que les soldes positifs et doivent être signalées comme repli partiel.
- L'activité observée vient des mouvements. `status = active` de Savings Account décrit la position administrative, mais ne prouve pas une activité transactionnelle.
- L'inactivité affichée est analytique (`actif`, `a_surveille`, `inactif_observe`, `historique_insuffisant`), pas une dormance réglementaire certifiée. Si Transactions atteint 100 000 lignes, conserver l'avertissement `historique_transactions_potentiellement_plafonne`.
- Le taux annuel DAT reste 11 % par défaut, paramétrable, et 0 % est autorisé pour désactiver l'estimation. La formule est `capital * taux_annuel / 100 * duree_contractuelle_jours / 365`; le résultat est une estimation de préparation, pas une écriture officielle.
- Ne calculer ni renouvellement DAT, ni churn certifié, ni part digitale sans source et règle fiables; marquer ces KPI en `data_gap`.
- Les opportunités `DAT sans crédit actif` et `forte épargne sans crédit` sont des pistes commerciales prudentes. Elles ne constituent jamais une éligibilité crédit automatique.
- Dans les concentrations, conserver le classement individuel (`rang_client`, `rang_encours` ou rang de volume) et ajouter une lecture par `tranche_encours`, toujours par devise et par famille d'encours. La tranche répond à la question "dans quelle classe d'argent se trouve le client"; elle ne remplace pas le rang du top clients.
- Les montants, soldes, flux, concentrations et estimations restent séparés par devise. Ne jamais additionner CDF et USD.
- Les sous-sous-onglets attendus sont simplifiés en six volets : `Vue d'ensemble`, `Flux et activité`, `Portefeuille et produits`, `DAT et échéances`, `Concentration et opportunités`, `Contrôles et anomalies`. Conserver toutes les analyses métier existantes, mais regrouper les blocs proches pour éviter une navigation trop longue.
- Les filtres de devise, famille, produit, statut, tranche, client et téléphone doivent prioriser les `multiselect`. Les notions comme horizon, taux annuel, inactivité observée, concentration, seuil forte épargne et data gap doivent avoir une infobulle claire.
- Chaque bloc analytique de l'onglet `Épargnes` doit afficher une aide compacte au survol ou une aide contextuelle équivalente : expliquer la source utilisée, l'objectif du bloc, la limite principale et rappeler la séparation des devises lorsque des montants sont affichés.
- Dans `Épargnes > Concentration et opportunités > Opportunités commerciales prudentes`, prioriser les colonnes dans cet ordre pour l'affichage et l'export CSV depuis le tableau : `id`, `numero_compte`, `id_client`, `numero_telephone`, `nom_client`, `famille_epargne`, `type_compte`, `id_produit_epargne`, `produit_epargne`, `description_produit_epargne`, `devise`, `solde`, `statut_compte`, `date_cloture`, `date_approbation`, `date_activation`, `interet_calcule`, `date_dernier_calcul_interet`, `date_prochain_calcul_interet`, `date_echeance`, `date_creation`, `date_mise_a_jour`, `date_creation_compte`, `date_situation`, `jours_avant_echeance`, `duree_contractuelle_jours`, `taux_interet_annuel_pct`, `interet_estime`, `capital_plus_interet_estime`, `tranche_echeance`. Les autres colonnes utiles restent disponibles après ces colonnes prioritaires.
- Lorsque l'option globale `Renommer automatiquement les colonnes` est active, les tableaux et exports Excel de l'onglet Épargnes utilisent des noms français, minuscules, sans accents et en `snake_case`.

## Invariants G2/DAT

- Autoriser G2/DAT sans fichier G2 lorsque Transactions M-PESA_Turbo est disponible. Dans ce mode, construire une ligne analytique par `ref_no` pour les dépôts/DAT/remboursements et par `reference_id + created_at` pour `Retrait Vers M-Pesa`; utiliser `created_at` comme date d'analyse et tracer `source_analytique = Turbo seul`.
- En mode Solution Numérique prioritaire, lorsque `Transactions` est disponible, piloter la période, les montants, les soldes, les DAT et les remboursements depuis la Solution Numérique, même si un fichier G2 est chargé. G2 ne doit jamais raccourcir la période analysée : il sert à enrichir l'identité du client par téléphone ou `Receipt No` et à fournir une preuve de rapprochement lorsque sa couverture temporelle le permet.
- Si G2 est chargé mais s'arrête avant la période Solution Numérique analysée, conserver les opérations Solution Numérique récentes dans le rapport `G2 / DAT`, enrichir les noms depuis l'historique G2 quand le téléphone est retrouvé, et classer les opérations hors couverture dans les lignes `à vérifier` avec le motif `Controle G2 indisponible sur la periode`. Ne pas recalculer les montants depuis G2.
- En mode Solution Numérique seule, ne jamais inventer `Opposite Party`, nom, statut, solde, `Initiation Time` ou `Completion Time` G2. Marquer les contrôles indépendants G2/Solution Numérique et le rapprochement comme `Non applicable - Solution Numérique seule`. Si aucun historique G2 n'est chargé, conserver le téléphone Solution Numérique comme identité minimale.
- En mode Solution Numérique seule, le rapport `G2 / DAT` doit rester réconciliable avec `Statistiques` sur le même périmètre : `Depot normal` = `depots_epargne_courante`, `DAT` = `depots_dat` et `Remboursement prets` = `remboursements_observes`, toujours par devise. Pour un remboursement de prêt, sommer les lignes `MPESA ACCOUNT` de remboursement portant le même `ref_no`; ne pas retenir seulement le maximum d'une ligne, car certains remboursements sont ventilés sur plusieurs écritures M-PESA.
- Conserver une ligne analytique canonique par `Receipt No.`; signaler tout reçu dupliqué et ne pas le compter deux fois.
- Accepter plusieurs relevés G2 simultanément, notamment les fichiers d'entrées et de sorties; conserver le fichier source avant de les unifier.
- Compter les transactions terminees par date, jour de semaine et heure de `Completion Time`, sur des grilles completes de lundi a dimanche et de 00h a 23h; conserver la separation par devise et par sens et afficher les periodes sans activite a zero.
- Rapprocher d'abord `Receipt No.` avec `ref_no` du Portal/Turbo. Agréger les écritures techniques du même `ref_no` sans additionner les miroirs comptables comme plusieurs opérations G2.
- Pour une sortie `BisouBisouB2C` sans `ref_no`, autoriser le repli uniquement vers `Retrait Vers M-Pesa` avec téléphone, devise et montant identiques et un écart maximal de 120 minutes. Identifier l'opération Turbo par `reference_id + created_at`; ne jamais utiliser `reference_id` seul, car il peut désigner un compte réutilisé.
- Classifier les entrées rapprochées avec `account_type` et `description` du Portal : `FIXED SAVINGS`/`Depot Bloque` = `DAT`, `NORMAL SAVINGS`/`Epargne depot` = `Depot normal`, compte prêt/principal/portefeuille = `Remboursement prets`.
- Utiliser les règles G2 comme repli lorsque le Portal ne contient pas la référence; classifier les sorties B2C, demandes de crédit et opérations internes selon `Details`, `Reason Type`, `Paid In` et `Withdrawn`.
- Comparer la création avec `Initiation Time` G2 contre `created_at` Turbo; utiliser `Completion Time` seulement comme repli de création, puis comme date de finalisation et source du délai de traitement. Une différence absolue supérieure à 60 minutes est une anomalie de date, même si les deux horodatages sont le même jour. Tolérer un passage de date jusqu'à 60 minutes en conservant les deux dates dans `Observation`. Un délai G2 négatif est une anomalie.
- Contrôler séparément téléphone, devise, montant et date. Distinguer `Rapproche exact`, `Rapproche avec ecart`, `Non rapproche` et `Non applicable - operation interne`. Une `Super Transaction` non rapprochée ne constitue pas, à elle seule, une anomalie client.
- Dès qu'un export fournit des statuts, retenir uniquement les statuts G2 explicitement terminés dans les synthèses financières, le rapprochement DAT, Perfect_client et le Word; conserver les autres lignes dans la répartition des statuts, le détail Excel et les anomalies. Un ancien export sans aucun statut reste compatible.
- Utiliser `G2_CLASSIFIED_TRANSACTION_COLUMNS` comme ordre du noyau métier du tableau `Transactions` et du Word : `date`, `receipt_no`, `currency_code`, `details_rapport`, `opposite_party`, `duree`, `compte_cree`, `montant`, `montant_entree`, `montant_sortie`, `balance_numeric`. L'écran peut ajouter le fichier source et le statut comme colonnes de contrôle.
- Appliquer les bornes inclusives de date et d'heure sur le champ temporel qui pilote réellement le rapport : `created_at` en mode Solution Numérique prioritaire ou seule, `Completion Time` en mode G2 pur. Appliquer ensuite le filtre de sens avant les synthèses, contrôles et exports; une sélection vide du multisélecteur de sens signifie tous les flux.

## Règles client et sources facultatives

La Solution Numérique constitue la source opérationnelle principale de la Solution M_PESA. G2 enrichit l’identité du client et fournit une preuve de rapprochement des écritures, sans intervenir dans le calcul des montants, des soldes, des DAT ou des remboursements.

- Considérer la Solution Numérique comme la source financière principale de l'Extrait client. Transactions fournit les mouvements et remboursements; `Savings Account` fournit les DAT en cours.
- Construire la recherche, l'extrait, la synthèse et les exports depuis la Solution Numérique même si G2 est absent; afficher alors explicitement `Solution Numérique seule` et réduire le contrôle G2 sans bloquer le client.
- Utiliser G2 uniquement comme source facultative de vérification et de complément du nom : enrichir la Solution Numérique par téléphone normalisé et, quand disponible, par `Receipt No = ref_no`. Ne jamais remplacer les montants, dates, soldes ou mouvements de l'extrait par G2; une divergence reste un résultat de contrôle.
- Construire la colonne `Description` de l'extrait officiel depuis les valeurs brutes `description` de Transactions M-PESA_Turbo, agrégées au grain de l'opération. Ajouter éventuellement téléphone et nom G2 après le libellé Turbo; ne jamais substituer `Details` ou `Reason Type` G2 à la description Turbo.
- Dans le relevé bancaire visible, construire `Détail des transactions` depuis le compte ouvert (`NORMAL SAVINGS`) uniquement : `cr` devient `Entrée`, `dr` devient `Sorties`, et `Solde` suit ces lignes affichées. Les comptes 1441/15558 restent utiles aux contrôles et rapprochements, mais ne doivent plus créer une lecture séparée du relevé.
- Séparer trois familles dans l'aperçu et les exports : mouvements du compte ouvert, situation de l'épargne/DAT et remboursements observés. Un remboursement financé par `NORMAL SAVINGS` déplace l'argent du compte ouvert vers le crédit : le conserver dans `Remboursements observés` et dans `Détail des transactions` car il touche le compte ouvert du client.
- Pour un octroi de crédit, conserver trois montants Turbo distincts : `prêt brut`, `intérêt prélevé` et `net versé`. Le taux standard Bisou Bisou est 7 % : `intérêt = prêt brut × 7 / 100` et `net versé = prêt brut - intérêt`. Dans le cas de référence de 5 USD, restituer 5,00 USD brut, 0,35 USD d'intérêt et 4,65 USD net.
- Dans l'Extrait client Word/PDF, comptabiliser uniquement le net versé comme sortie Bisou Bisou et utiliser le libellé `Prêt brut : 5,00 USD — intérêt prélevé : 0,35 USD (7 %) — net versé : 4,65 USD`, avec les montants et la devise réels de l'opération. Les lignes `MPESA ACCOUNT` Turbo restent la preuve du net; G2 confirme éventuellement le versement sans fournir le montant de calcul.
- N'afficher le détail brut/intérêt/net que lorsque Turbo porte les deux faces `MPESA ACCOUNT`. Si la ligne d'intérêt manque dans un export partiel, ne pas inventer 7 % ni réduire artificiellement le versement observé; conserver le libellé Turbo et signaler l'incomplétude dans les contrôles.
- Ne jamais regrouper l'octroi et son remboursement sur le seul `reference_id` du prêt. Séparer les événements par `created_at` et `ref_no` : le scénario `PRET_TEST_001` contient 12 écritures d'octroi à 16:17:16 et 4 écritures de remboursement portant ce `reference_id` à 16:26:43, soit 16 lignes pour le même `reference_id`. L'événement de remboursement contient aussi 2 lignes techniques `NORMAL SAVINGS`/`FIXED SAVINGS` sans ce `reference_id`, donc 6 écritures au total.
- Présenter l'Extrait client, son aperçu et ses exports comme un relevé bancaire professionnel unique. Ne pas afficher `Situation financière actuelle du client` ni le solde du portefeuille M-PESA comme bloc séparé. Le titre visible reste `Extrait de compte` ou `Extrait minimal de compte`.
- Conserver deux formats d'export seulement dans `Extrait client` : `global` pour le relevé complet et `minimal` pour le relevé court. Le format global garde les blocs utiles de contrôle et d'analyse; le format minimal garde l'en-tête, les critères, la synthèse financière et le détail des transactions.
- Dans le relevé, ne jamais utiliser `Cumul net des flux` comme solde client. Le solde du détail suit uniquement le compte ouvert (`NORMAL SAVINGS`), tandis que `Compte ouvert` et `Compte bloqué` viennent de `Savings Account`. Pour le scénario Benjamin `243000000000`, le relevé attend 0,00 USD en compte ouvert et 10,00 USD en compte bloqué; le prêt de 4,65 USD et les frais/intérêts de 0,35 USD ne doivent pas créer un solde de compte ouvert.
- Conserver les positions M-PESA, épargne, DAT et crédit séparées. Ne jamais compenser un crédit avec l'épargne ni additionner un remboursement interne aux flux externes. Lors d'un remboursement depuis le compte ouvert, ne pas interpréter une ligne technique `FIXED SAVINGS` portant le même libellé comme une sortie du DAT.
- Placer immédiatement après les critères `Synthèse financière par devise`, avec uniquement `Devise`, `Ouverture`, `Entrée`, `Sorties`, `Cloture`, `Compte ouvert` et `Compte bloqué`. Ne pas afficher `Flux net externe`. `Ouverture + Entrée - Sorties = Cloture` doit suivre le compte ouvert du client; `Compte bloqué` reste la position DAT séparée.
- Remplacer `Compte :` par `Devise :` dans les critères de l'en-tête Word. Conserver la colonne `Compte` dans le tableau transactionnel pour montrer 1441 ou 15558 ligne par ligne.
- Proposer les exports Word `CDF`, `USD` et `ALL`. Dans `ALL`, afficher la devise sur chaque ligne et calculer séparément les flux par devise; ne jamais produire de total CDF + USD.
- Sélectionner par défaut les dépôts, les retraits `Retrait Vers M-Pesa`, les décaissements de crédit et les remboursements de crédit dans l'Extrait client. Regrouper un retrait au grain `customer_id + devise + created_at + reference_id` lorsque `ref_no` est absent, afin de ne pas compter deux fois les lignes miroir `MPESA ACCOUNT` et `NORMAL SAVINGS`.
- Limiter le tableau de vérification G2 au seul `customer_id` sélectionné, y compris lorsque le fichier DAT est absent.
- Résoudre vers `customer_id` après rapprochement; utiliser MSISDN ou référence uniquement selon les règles documentées.
- Rechercher `compte_cree` dans `Clients_Turbo`, puis l'épargne courante, puis le DAT.
- Agréger Perfect par `Phone_Prefixe` avant la jointure et conserver le nombre d'identités associées au numéro.
- Construire l'intersection G2–Turbo–Perfect au grain d'un téléphone normalisé, avec `present_dans_turbo`, `present_dans_g2`, `present_dans_perfect`, `present_dans_les_3_systemes` et le dataset `clients_trois_systemes`.
- Dans les libellés utilisateur, distinguer `Clients_Turbo`, `Clients_Perfect` et les clients transactionnels déduits de Turbo/G2; ne jamais appeler ces derniers `Clients_Turbo` sans preuve dans le fichier correspondant.
- Produire trois populations inclusives : `clients_perfect_dans_mpesa` pour Perfect∩G2, `clients_perfect_dans_turbo` pour Perfect∩Turbo et `clients_perfect_dans_turbo_et_mpesa` pour Perfect∩Turbo∩G2.
- Réduire proprement le rapport lorsqu'une source facultative manque; ne jamais provoquer un `KeyError` en indexant une source absente.
- Nommer la référence du détail `Référence`; réserver `Receipt No.` aux colonnes G2 de contrôle. `Détail des transactions` couvre les opérations bancaires utiles au compte client : dépôt, retrait, retrait de DAT avant échéance, retour du montant principal du DAT, entrée des intérêts sur DAT et remboursement d'un crédit depuis le compte ouvert. Exclure de ce détail les décaissements de prêt, car le prêt passe du compte prêt Bisou Bisou vers le compte M-PESA du client sans toucher le compte ouvert; exclure aussi les dépôts DAT initiaux, qui restent dans `Éléments couverts par l'extrait client` et `DAT en cours`. Utiliser les colonnes `Entrées`, `Sorties` et `Solde`; ce solde reprend `Ouverture` puis suit les mouvements affichés jusqu'à `Cloture`.
- Dans l'Extrait client, construire `dat_en_cours_client` depuis les `FIXED SAVINGS` à solde strictement positif de `Savings Account`, au grain `savings_id`, filtrés par client et devise. Ne pas appliquer la période transactionnelle à cette position courante.
- Dater la situation depuis `updated_at` ou `date_locked` de `Savings Account`; à défaut, utiliser la dernière transaction Turbo du client, puis les dates du DAT. Ne jamais utiliser une date G2 pour la situation DAT.
- Calculer l'intérêt simple estimé au taux DAT paramétré, 11 % par défaut, entre `date_approved` et `maturity_date`. Qualifier chaque DAT `En cours`, `Échéance proche`, `Échéance aujourd'hui` ou `Échu à rembourser`. Ne jamais présenter l'estimation comme une écriture comptable.
- Construire `remboursements_turbo_synthese_client` et `remboursements_turbo_detail_client` uniquement depuis les événements de remboursement de Transactions M-PESA_Turbo. Exclure les décaissements, la dette créée et les positions de crédit de la restitution Extrait client.
- Dans l'export global de l'Extrait client, ajouter `Prochains remboursements sur la période` depuis `Loans Account [Turbo]`, filtré par `due_date`, client et devise. Afficher seulement les échéances utiles à préparer : date prévue, crédit, devise, montant à rembourser, principal, intérêts, pénalités et statut. Ne pas inclure ce bloc dans le format minimal.
- Couvrir dans le `Détail des transactions` les six familles attendues : dépôt, retrait, retrait de DAT avant échéance, retour du montant principal du DAT, entrée des intérêts sur DAT et remboursement d'un crédit depuis le compte ouvert. Les dépôts/retraits/remboursements depuis compte ouvert viennent des écritures `NORMAL SAVINGS`; les retours de principal DAT viennent des événements Turbo `Retrait Compte Bloque`; les intérêts DAT constatés viennent exclusivement de `Savings Account.interest_earned` sur un DAT dénoué.
- Classer `Sortie M-PESA_Turbo vers DAT` dans `Dépôt à terme (DAT)` et l'afficher dans `Éléments couverts par l'extrait client`. Ne pas répéter cette opération dans `Détail des transactions`, car le dépôt initial DAT concerne le compte bloqué et non le compte ouvert. Le bloc `DAT en cours` reste une position distincte, alimentée uniquement par `Savings Account` lorsqu'il est disponible.
- Distinguer l'origine du remboursement : utiliser `MPESA ACCOUNT` pour `Compte M-PESA`; utiliser le mouvement `NORMAL SAVINGS` portant un libellé de remboursement de compte pour `Compte ouvert`. Ne jamais déduire cette origine depuis G2.
- Conserver les retours de capital DAT et les intérêts DAT hors du calcul du solde M-PESA lorsqu'aucune ligne `MPESA ACCOUNT` ne les porte. Dans le bloc `Retours du capital mis en DAT`, afficher la date et l'heure de création du DAT ainsi que la date et l'heure de sa fin. Relier le retour à la ligne `FIXED SAVINGS` par `reference_id`, privilégier le `Savings Account` correspondant pour la création, puis utiliser le dépôt bloqué Turbo de même référence comme repli; ne jamais associer arbitrairement un DAT parmi plusieurs candidats. Dans les documents client, limiter la section d'intérêts à l'échéance, au `savings_id`, à la devise, au capital placé, à l'intérêt client et à capital + intérêt; ne pas afficher la part Vodacom ni les colonnes techniques de traçabilité.
- Ne jamais modifier les fichiers Excel sources pendant l'analyse.
- Traiter l'export détaillé `Savings Account` comme source maître lorsqu'il est fourni. Déduire `NORMAL SAVINGS` de `Open Savings` / `Current account` et `FIXED SAVINGS` des produits `Fixed Account`; conserver les DAT à solde nul comme historique.
- Utiliser l'export `Customers with Current Savings Account` comme vue des comptes courants à solde positif; il ne représente pas les comptes courants à solde nul. Sans source maître, l'accepter seulement avec le résumé Fixed dans le téléversement unique et afficher le mode partiel.
- Utiliser l'export `Customers with Fixed Savings Account` comme vue des DAT à solde positif. Sans source maître, l'accepter avec le résumé Current et alimenter les analyses DAT disponibles en signalant l'absence des DAT à solde nul; avec la source maître, ne pas le recompter.
- Accepter les relevés G2 commençant directement par `Receipt No.` et les exports organisation bruts contenant cinq lignes descriptives. Promouvoir automatiquement la vraie ligne d'en-tête séparément pour chaque fichier 1441/15558.

## Invariants Finance et comptabilité — pilotage financier

- Pour Transactions Turbo, conserver la sémantique comptable : `dr` = sortie du compte M-PESA et `cr` = entrée. Ne jamais appliquer les règles G2 `Paid In`/`Withdrawn` aux fichiers Turbo.
- Dédupliquer les transactions Turbo par `id`, les crédits par `loan_id`, les clients par identifiant ou téléphone/date, les comptes d'épargne et DAT par leur clé de compte; conserver la version la plus récente et la liste des fichiers sources.
- Construire chaque événement métier par `ref_no`; quand il manque, regrouper `customer_id + devise + created_at`. Ne jamais compter les écritures miroir comme plusieurs opérations.
- Utiliser une période inclusive `date début - date fin` et proposer la dernière journée opérationnelle complète. Permettre les évolutions par jour, semaine ou mois.
- Calculer exclusivement depuis Transactions M-PESA_Turbo les dépôts, retraits, remboursements observés, décaissements de nouveaux crédits, flux nets, activité d'épargne, concentration des transactions, transactions importantes, activité inhabituelle et fractionnement potentiel.
- Dans `Finance et comptabilité > Nouveaux crédits`, conserver le montant brut accordé. Dans les produits financiers, présenter séparément l'intérêt prélevé et permettre d'ouvrir le détail par écriture, référence, client et devise; ne jamais remplacer le crédit brut par le net client ni additionner deux fois l'intérêt.
- Utiliser Loans Account_Turbo pour l'encours, les positions de crédit et le PAR 1/7/30 simplifié depuis `due_date`. Laisser le PAR vide si l'encours ou l'échéance nécessaire manque; ne jamais prétendre disposer d'un plan d'amortissement détaillé.
- Rapprocher globalement les décaissements Turbo des comptes de crédit créés dans la période, par devise. Présenter l'écart comme un contrôle global et non comme une affectation ligne à ligne.
- Adapter les contrôles Perfect Vision prioritaires réellement démontrables : remboursements, évolution dépôts/crédits, nouveaux crédits, concentration crédit/transactions, PAR par tranche, dépôts fréquents, tranches de dépôts, comptes inactifs, DAT sans crédit actif et crédit avec épargne disponible. Signaler les analyses non reproductibles faute de plan d'amortissement, provision, garantie ou plan comptable complet.
- Conserver G2 hors de tous ces calculs. Il peut enrichir un nom et prouver un rapprochement ailleurs dans la solution, mais ne modifie aucun montant, solde, DAT, encours, remboursement, seuil ou alerte du cockpit.
- Préparer les sources communes une seule fois après téléversement, puis calculer les analyses lourdes uniquement lorsque le sous-onglet ou le volet concerné est ouvert. Conserver les résultats déterministes en cache borné afin d'éviter les recalculs inutiles sans saturer la mémoire Streamlit Cloud. Mettre en cache séparément le journal d'événements Turbo et le rapprochement crédit-épargne; une modification de période doit réutiliser la consolidation initiale lorsque c'est possible.
- Traiter les alertes comme des signaux de revue et non comme des preuves de fraude ou d'erreur.
- Produire l'échéancier DAT par tranche et devise.
- Dans le sous-onglet DAT, lister en priorité les comptes à solde positif déjà échus et ceux arrivant à terme dans un horizon réglable, fixé à 30 jours par défaut. Conserver `savings_id`, client, téléphone, produit, statut, approbation, échéance, jours restants, capital, intérêt estimé et capital plus intérêt.
- Utiliser 11 % comme taux d'intérêt annuel DAT Bisou Bisou par défaut dans la barre latérale. Autoriser sa modification et calculer l'intérêt simple estimé par `capital × taux annuel × durée contractuelle en jours / 365`, de `date_approved` à `maturity_date`.
- Présenter le capital, l'intérêt et le remboursement estimé séparément par devise. Qualifier ces montants d'estimations de préparation et non d'écritures comptables officielles.

## Invariants crédit et épargne

- Conserver `Loans Account` comme source obligatoire des prêts, encours, remboursements, intérêts, frais, échéances et statuts. `Savings Account` seul ne permet jamais de reconstruire un crédit.
- Rapprocher d'abord `Loans.savings_account_id` avec `Savings Account.id` ou `Savings Account.savings_id` lorsqu'il est renseigné et unique. Sinon, déduire le compte courant uniquement lorsque `customer_id + currency_code` identifie exactement un `NORMAL SAVINGS`.
- Conserver dans le contrôle les identifiants directs introuvables, les absences de compte courant, les correspondances multiples et les écarts de client, devise ou téléphone. Qualifier la liaison de `directe` ou `déduite`; ne jamais présenter une liaison déduite comme contractuelle.
- Construire la vue consolidée au grain `customer_id x devise`. Compter l'épargne courante et les DAT une seule fois par client et devise, même si le client possède plusieurs prêts.
- Juxtaposer montant du crédit, remboursements, encours, épargne courante et DAT positifs. Ne jamais compenser comptablement l'épargne avec le crédit, assimiler l'épargne à une garantie ou additionner CDF et USD.
- Cas réel du 17 juillet 2026 : `savings_account_id` est vide sur les 2 213 crédits. Le repli client x devise rapproche 2 212 crédits; un crédit USD sans compte courant correspondant reste à revoir.

## Invariants Finance et comptabilité — comptabilité observée

- Utiliser exclusivement Transactions M-PESA_Turbo pour les écritures, les débits, les crédits, les soldes observés et les journaux comptables. G2 sert seulement à compléter le nom du client et à mesurer le rapprochement `Receipt No = ref_no`; ses montants ne remplacent jamais Turbo.
- Construire la balance auxiliaire client sur les comptes produits `NORMAL SAVINGS`, `FIXED SAVINGS` et `PRINCIPLE`, au grain `customer_id x devise x famille de position`. Résoudre une référence absente uniquement lorsqu'un seul compte connu du même type existe pour le client; sinon conserver `Reference compte ambigue ou absente`.
- Présenter tous les autres types de compte dans la balance des mouvements et le journal Turbo. Ne pas additionner les sous-registres techniques d'un crédit comme s'ils formaient une écriture unique : Turbo peut décrire plusieurs couches comptables de la même opération.
- Regrouper les opérations d'abord par `ref_no`; lorsque la référence manque, utiliser `customer_id + devise + created_at`. Conserver le journal brut, le journal regroupé, le contrôle de symétrie débit/crédit et le contrôle d'amplitude `abs(bal_after - bal_before) = abs(dr) + abs(cr)`.
- Qualifier chaque ouverture, clôture et position comme `observée`. Sans plan comptable complet ni soldes d'ouverture officiels, ne jamais appeler cette restitution `balance générale certifiée`, bilan ou compte de résultat officiel.
- Présenter séparément les intérêts, pénalités, parts Bisou et parts Voda observées. Ne pas les additionner automatiquement, car ces lignes peuvent constituer plusieurs ventilations d'un même produit financier.
- Afficher les positions des instantanés Current Savings, Fixed Savings et Loans à part de la balance journalière : leur date d'extraction peut être postérieure à la période comptable filtrée.
- Calculer et exporter toutes les synthèses par devise. Ne jamais compenser ou totaliser CDF et USD.
- Dans `Finance Turbo > Balances et journaux`, proposer une exportation Word et PDF compacte de la balance observée destinée à la Direction. Inclure le logo, la période, la synthèse par devise et la balance par client. Ne pas inclure dans ces documents le paragraphe explicatif `Source des montants...` ni le tableau `Balance des mouvements par type de compte`; ce dernier reste disponible à l'écran et dans les données comptables techniques.
- Pour contrôler une évolution de la comptabilité Turbo, relire le cas de référence clôturé du 16 juillet 2026 dans [references/data-contracts.md](references/data-contracts.md). Comparer le grain, les contrôles et les montants par devise; ne pas transformer ces valeurs historiques en seuils métier permanents.

## Architecture Streamlit des sous-onglets

- Pour toute période utilisateur, afficher deux `st.date_input` distincts intitulés exactement `Date de début` et `Date de fin`, au format `DD/MM/YYYY`. Ne jamais utiliser un `st.date_input` initialisé avec un tuple ou une liste comme sélecteur de plage. Employer deux clés stables distinctes, valider `date_debut <= date_fin` et appliquer des bornes inclusives.
- Construire la navigation principale avec `st.tabs(..., on_change="rerun")` et rendre uniquement le sous-onglet ouvert. Les libellés restent immédiatement disponibles, mais les contenus lourds cachés ne doivent pas se calculer en arrière-plan.
- Organiser les sous-onglets principaux dans cet ordre : `Importation et contrôle`, `Extraits clients`, `Finance et comptabilité`, `Épargnes`, `Crédits`, `Solution Numérique / M-Pesa`, `Perfect Client`, `Statistiques`, `Projections`.
- Fusionner `Controle des donnees` dans `Importation et contrôle` : l'onglet d'importation affiche les fichiers charges, les colonnes attendues/manquantes, la composition Savings Account, les controles de donnees et les anomalies Transactions [Turbo].
- Réunir le pilotage financier et la comptabilité observée dans un seul sous-onglet principal `Finance et comptabilité`. Utiliser une période et une sélection de devises communes, puis six volets internes : `Vue direction`, `Flux et activité`, `Crédit, épargne et DAT`, `Balances et journaux`, `Risques et contrôles` et `Export`.
- Dans `Finance et comptabilité`, tout tableau nominatif qui contient une colonne de téléphone (`telephone`, `msisdn1`, `msisdn`, `telephone_credit`, `telephone_epargne`, `phone_prefixe` ou équivalent) doit proposer ce champ comme filtre `multiselect`, au même niveau que la devise, le client, le statut ou le contrôle. Le téléphone est une clé principale de recherche et ne doit pas être seulement affiché dans le tableau.
- Dans `Épargnes` et `Crédits`, appliquer la même règle : tout tableau détaillé, liste d'action, concentration, échéance, rapprochement ou portefeuille affichant un téléphone doit proposer ce téléphone en filtre `multiselect`. Le filtre téléphone ne doit pas être remplacé par le seul `customer_id`, car le numéro reste la clé terrain la plus rapide pour retrouver un client. Si plusieurs colonnes techniques de téléphone existent dans le même tableau (`telephone`, `msisdn`, `msisdn1`, `numero_telephone_msisdn`, etc.), afficher un seul filtre utilisateur nommé `numero_telephone` lorsque le renommage français est actif.
- Dans `Finance et comptabilité`, éviter de reconstruire plusieurs fois les mêmes agrégats : réutiliser le journal d'événements et les rapports mis en cache, et reporter les calculs non visibles lorsque cela évite un pic mémoire. Ne jamais réintroduire deux périodes concurrentes.
- Conserver deux contrats d'export séparés dans le volet `Export` : le classeur ciblé de pilotage et le classeur comptable à douze feuilles.
- Isoler chaque fonction de rendu avec `st.fragment`. Après le chargement initial, une interaction locale doit recalculer uniquement le sous-onglet concerné.
- Dans les sous-onglets lourds de Solution Numérique, regrouper les paramètres locaux de début de page dans un `st.form` et appliquer les valeurs uniquement avec un bouton `Actualiser ...`. Un changement de date, fréquence, seuil, horizon ou sens de flux ne doit pas relancer immédiatement les analyses tant que l'utilisateur n'a pas validé les paramètres. Dans `Statistiques`, aucun KPI, graphique, tableau ou export ne doit être calculé au premier affichage avant le clic sur `Actualiser les statistiques`.
- Pour les sous-onglets lourds (`Clients`, `Épargnes`, `Crédits`, `Solution Numérique / M-Pesa`, `Statistiques` et `Projections`), la période proposée par défaut ne doit jamais dépasser trois mois, soit 90 jours. Après un nouveau téléversement, ne pas réutiliser automatiquement une ancienne validation : l'utilisateur doit cliquer sur `Actualiser ...` pour lancer l'analyse du nouveau jeu de fichiers.
- Garder les téléversements et la préparation partagée en dehors des fragments : toute modification des fichiers sources déclenche volontairement une reconstruction complète des sous-onglets.
- Mettre en cache avec `st.cache_data` la lecture, la normalisation et les calculs déterministes lourds. Laisser les widgets et le rendu Streamlit hors du cache.
- Ne pas faire hacher les grands DataFrames préparés à chaque interaction. Utiliser une empreinte compacte du contenu des fichiers comme clé de préparation, puis ajouter la période, le client ou le filtre aux clés des rapports dérivés. Borner `max_entries` pour éviter une croissance mémoire sans limite.
- Pour G2/Turbo, agréger uniquement les `ref_no` réellement présents dans G2 et les retraits B2C candidats situés dans la fenêtre utile. Le filtrage de performance ne doit jamais élargir ni réduire la tolérance métier de 60 minutes.
- Conserver le contexte client dans `Extrait client`. Alimenter DAT, G2/DAT, crédits et diagnostics depuis les données globales préparées afin qu'un fragment ne dépende pas de l'état local d'un autre sous-onglet.
- Organiser l'écran `Extrait client` comme un parcours de relevé professionnel : recherche client, critères de restitution, situation financière, filtres, aperçu du relevé, DAT en cours, remboursements observés, contrôles et exports. Faire correspondre l'aperçu écran aux blocs Word/PDF (`Synthèse financière par devise`, `Éléments couverts par l'extrait`, DAT, remboursements et détail), puis garder les graphiques, G2 et colonnes techniques dans des expanders.
- Construire `Clients` comme une brique Client 360 placée immédiatement après `Finance et comptabilité`. L'onglet couvre référentiel client, activité, acquisition, activation, produits détenus, DAT sans crédit actif, segmentation et opportunités. Lire aussi [references/kpi_clients.md](references/kpi_clients.md). Les volets attendus sont simplifiés en cinq blocs : `Vue d'ensemble`, `Activité et activation`, `Nouveaux clients et comptes`, `Client 360 et segmentation`, `Opportunités`.
- Dans `Clients`, calculer `clients_referentiel` depuis `Customers [Solution Numérique]` dédupliqué par téléphone normalisé; ne jamais utiliser le nombre brut de lignes lorsqu'il existe des doublons. `Customers.created_at` est la date de création client de cette source et prime sur les dates plus anciennes observées dans Savings, Loans ou Transactions.
- Calculer `clients_actifs` depuis les événements métiers consolidés de Transactions [Solution Numérique], en réutilisant `build_turbo_operation_events`; ne jamais compter les lignes comptables brutes comme opérations.
- Afficher explicitement le dénominateur de `taux_clients_actifs` : `clients_referentiel` si Customers est disponible, sinon `clients_connus_solution_numerique`. `taux_activation_nouveaux_clients` = `nouveaux_clients_actifs / nouveaux_clients`.
- Dans `Clients`, ajouter une analyse `Nouveaux clients et comptes actifs par devise` : retenir les clients créés dans `Customers.created_at` sur la période et les comptes ouverts/DAT créés dans `Savings Account` sur la période, puis indiquer par client et par devise s'ils ont eu des transactions, le nombre d'opérations consolidées, le volume observé, le solde de compte ouvert et le solde DAT. Les montants restent strictement séparés par `currency_code`; G2 peut enrichir le nom mais ne calcule ni activité, ni solde.
- Dans `Clients`, ajouter une synthèse `tranches d'encours` depuis les positions produit au grain client x devise x famille (`compte_ouvert`, `dat`, `credit`). Les tranches permettent d'analyser la structure des avoirs ou encours par niveau d'argent, sans additionner les devises et sans fusionner les familles de produit.
- Construire `Client 360` au grain client canonique : `customer_id` si disponible, sinon téléphone normalisé, avec `methode_rapprochement` et `statut_confiance`. Ne jamais rapprocher uniquement sur le nom. G2 peut enrichir le nom et contrôler, mais ne modifie aucun KPI financier ou client.
- Segmenter uniquement avec des règles objectives et visibles : `nouveau_actif`, `nouveau_non_active`, `actif`, `occasionnel`, `inactif_observe`, `multi_produits`, `sans_mouvement`, puis les combinaisons produits `epargne`, `dat`, `credit`. Les seuils d'inactivité et d'activité occasionnelle doivent être paramétrables à l'écran; `inactif_observe` n'est pas un statut réglementaire.
- Réutiliser `dat_sans_credit_actif` du cockpit financier pour la liste `clients_dat_sans_credit_actif`. Présenter cette liste comme un potentiel commercial crédit, jamais comme une éligibilité automatique.
- Dans `Clients`, les noms de colonnes visibles, les filtres et les exports doivent rester en français fonctionnel, avec des identifiants `snake_case` sans accent lorsque l'option `Renommer automatiquement les colonnes` est active. Le numéro de téléphone est une clé principale de lecture et doit être proposé comme filtre `multiselect` dans les listes nominatives. Les listes catégorielles et listes d'action doivent permettre une sélection multiple lorsqu'il est utile de comparer plusieurs groupes.
- Chaque bloc analytique de l'onglet `Clients` doit avoir une aide contextuelle courte expliquant l'objectif du bloc : référentiel, activité, acquisition, nouveaux comptes, Client 360, segmentation ou opportunités. Dans les tableaux nominaux, prioriser l'identification client devant les mesures : `client_key`, `customer_id`, `numero_telephone`, `nom_client`, dates de création, devise, présence produit, activité, soldes, segments, confiance et sources.
- Dans toute la Solution Numérique, prioriser `multiselect` pour les filtres de données, listes catégorielles, devises, statuts, contrôles, types d'opérations, segments et listes d'action. Conserver `selectbox` uniquement pour les choix réellement exclusifs : fréquence, mode d'affichage, horizon, intervalle de prévision, indicateur de graphique unique ou client exact à analyser.
- Les opportunités de l'onglet `Clients` sont exportées en Excel : clients actifs, nouveaux clients, nouveaux clients actifs, nouveaux clients non activés, clients sans mouvement, clients inactifs observés, clients DAT sans crédit actif et clients multi-produits. Les noms de colonnes de l'écran et de l'export suivent l'option globale `Renommer automatiquement les colonnes`.
- Construire `Statistiques` comme un cockpit Solution Numérique-first distinct de `Finance et comptabilité` : analyses clients, activite, croissance, volume, chiffre d'affaires observe, epargne/DAT, credit et top clients. L'ecran doit etre organise en blocs decisionnels professionnels repliables/depliables : `Clients`, `Comptes ouverts et comptes bloques`, `Credits` et `Transactions`, avec les KPI, graphiques et tableaux rattaches a leur famille metier. Les filtres professionnels minimaux sont `Date de debut`, `Date de fin`, `Frequence`, `Devises affichees` et `Top clients affiches`; ils doivent influencer les KPI, graphiques, tableaux et exports.
- Construire `Crédits` comme un cockpit de pilotage distinct mais non parallèle à `Finance et comptabilité`. Lire aussi [references/kpi_credit.md](references/kpi_credit.md). Les volets attendus sont simplifiés en six blocs : `Vue d'ensemble`, `Production et remboursements`, `Portefeuille et échéances`, `Risques et concentration`, `Crédit et épargne`, `Opportunités et qualité`. Conserver toutes les analyses métier existantes dans ces blocs regroupés. Dans la concentration crédit, conserver le top des prêts et des clients, puis ajouter une synthèse par `tranche_encours` client afin de lire le risque par niveau d'exposition.
- Dans `Crédits`, traiter `Loans Account` comme un instantané actuel : il fournit encours, statuts, dates d'échéance, défauts observés, rollover, période de grâce et position crédit. Il ne permet pas de reconstruire une courbe historique d'encours, de PAR ou de provisions sans plusieurs arrêtés datés.
- Calculer la production et les remboursements de crédit depuis les événements consolidés de Transactions [Solution Numérique]. Ne jamais compter les lignes comptables brutes comme des opérations et ne jamais remplacer le prêt brut du portefeuille par le net client.
- Afficher uniquement un `PAR simplifié` depuis `due_date` (`par_simplifie_1j`, `par_simplifie_7j`, `par_simplifie_30j`) avec une aide explicite. Ne pas ajouter PAR90/PAR180, aging détaillé, provision, collection efficiency, coût du risque, rendement sur encours moyen, garantie/caution ou restructuration sans source fiable; les classer dans le catalogue `data_gap`.
- Dans `Crédits > Crédit et épargne`, réutiliser le rapprochement `savings_account_id`, puis le repli prudent `customer_id + currency_code` lorsqu'un seul compte courant candidat existe. Présenter l'épargne comme une mise en regard analytique, jamais comme une garantie ni une compensation comptable.
- Dans `Crédits > Opportunités et qualité`, afficher une seule opportunité par défaut. Si plusieurs opportunités sélectionnées aboutissent au même tableau après filtres, signaler la liste équivalente et ne pas répéter visuellement les mêmes lignes; conserver cependant chaque liste dans l'export Excel pour garder la traçabilité métier.
- Dans `Crédits`, les listes d'action minimales sont : prêts échus avec encours, prêts `defaulted`, prêts avec pénalités, prêts PAR simplifié 30 jours, fortes expositions, prêts arrivant à échéance dans 30 jours et rapprochements épargne ambigus.
- Chaque bloc analytique de l'onglet `Crédits` doit afficher une aide contextuelle courte expliquant la source, l'objectif et la limite de lecture : portefeuille depuis `Loans Account`, production/remboursements depuis `Transactions`, risque simplifié depuis `due_date`, concentration par devise, rapprochement crédit-épargne sans compensation et opportunités à revue humaine. Dans les tableaux détaillés et listes d'action, prioriser les colonnes d'identification du prêt et du client devant les mesures : `id`, `loan_id`, `customer_id`, `numero_telephone`, `nom_client`, `currency_code`, produit, compte épargne lié, montant accordé, encours, montant payé, principal/intérêts/frais/pénalités restants, statut, défaut, échéance, retard, tranche et dates de création/mise à jour. Le nom client doit être visible dès qu'il existe dans les sources, même si la colonne source s'appelle `customer`, `Nom_client`, `nom_client` ou provient d'un enrichissement G2; si plusieurs tableaux Crédits n'ont que `customer_id`, compléter l'affichage depuis le référentiel des prêts chargés sans modifier les calculs.
- Dans `Statistiques`, classer l'importance des sources ainsi : `Transactions [Turbo]` indispensable, `Savings Account [Turbo]` indispensable, `Loans Account [Turbo]` tres important, `Customers [Turbo]` important, `Transactions [G2]` facultatif utile, `Clients_Perfect` facultatif analytique.
- Dans le bloc `Transactions` de `Statistiques`, ajouter une section distincte `Qualité du rapprochement G2`. Qualifier la couverture `Complète - entrées 1441 et sorties 15558`, `Partielle - entrées 1441 uniquement`, `Partielle - sorties 15558 uniquement` ou `G2 absent`. Cette section ne doit modifier aucun KPI Turbo.
- Mesurer par devise et par circuit les opérations G2, les opérations terminées, les rapprochements, les non-rapprochements et le taux de rapprochement. Pour 1441, utiliser `Receipt No. = ref_no`; pour les sorties B2C 15558, utiliser le repli téléphone + devise + montant + heure. Conserver les statuts G2 et la liste des opérations terminées comparables non rapprochées.
- Traiter `BisouBisouLoanRequest` séparément des sorties B2C. Ne jamais compter une demande de crédit comme une sortie B2C non rapprochée; afficher un contrôle spécifique `prêt brut Turbo / intérêt observé de 7 % / net versé G2`.
- Comparer, selon l'horizon choisi et par devise, le taux global de rapprochement G2, le taux des entrées 1441 et le taux des sorties B2C 15558. Exprimer l'écart en points de pourcentage et signaler une couverture temporelle partielle lorsque les deux périodes requises ne sont pas disponibles.
- Calculer `Clients Turbo actifs` depuis les operations Transactions [Turbo] de la periode. Dans l'écran et le Word, nommer `Clients du fichier Customers [Turbo] chargé` les clients distincts présents dans le référentiel Customers [Turbo] chargé, avant le filtre de date de fin; cet indicateur sert à rapprocher le rapport avec le fichier Excel source. Calculer `Clients Turbo connus à la date de fin` depuis `Customers [Turbo]` créé avant ou à la date de fin du rapport, sinon depuis les clients observes dans les sources Turbo disponibles. Construire la courbe de croissance depuis `Customers [Turbo].created_at` lorsque disponible, sinon depuis la premiere observation Turbo. Ne jamais présenter le nombre brut de lignes Excel comme base client sans déduplication ni périmètre de date.
- Afficher le `Chiffre d'affaires observe` comme indicateur prudent et non certifie : interets + penalites + part Bisou detectes dans Transactions [Turbo], toujours separes par devise. Ne jamais utiliser G2 ou Perfect pour ce calcul.
- Ajouter dans le bloc de barre latérale `Référence et stockage`, sous `Paramètres de calcul`, un sélecteur global `Période de comparaison M_PESA` avec `7 jours glissants`, `15 jours glissants`, `30 jours glissants` et `Période filtrée`. Le mode par défaut est `7 jours glissants`; ne plus proposer `Semaine microfinance (lundi)`. Ne pas créer une deuxième section latérale propre à Solution M-PESA. `Période filtrée` reprend exactement `Date de début - Date de fin` et la compare à la période immédiatement précédente de même durée.
- Ajouter au même emplacement un sélecteur global `Périmètre annuel M_PESA` avec `Ensemble des années`, `Année unique` et `Plage d'années`. Garder `Ensemble des années` par défaut : tous les fichiers restent entièrement chargés et mis en cache, puis le moteur construit une vue analytique non destructive. Pour Transactions Turbo et G2, filtrer strictement du 1er janvier de l'année de début au 31 décembre de l'année de fin. Pour les instantanés Savings Account, DAT, Loans Account et Customers, conserver les positions créées ou activées au plus tard à la fin du périmètre, y compris celles d'années antérieures encore actives; ne jamais faire disparaître un compte ancien uniquement parce qu'il précède l'année de début. Conserver les lignes sans date et signaler la limite d'historisation. `Importation` et ses contrôles restent fondés sur les sources complètes; tous les autres sous-onglets utilisent la vue annuelle.
- Les filtres locaux `Date de début` et `Date de fin` restent applicables à l'intérieur du périmètre annuel. Le rapport Word `Statistiques` doit afficher le `Périmètre annuel` dans ses critères. Une plage inversée est normalisée de l'année la plus ancienne à l'année la plus récente.
- Ajouter aux KPI une comparaison Turbo entre les deux fenêtres produites par ce sélecteur. Afficher la valeur courante, la valeur précédente et l'évolution en pourcentage, avec les dates exactes des deux fenêtres et le mode retenu. Ne jamais appeler systématiquement cette restitution `Semaine analysée` lorsque l'horizon sélectionné n'est pas hebdomadaire.
- Dans `Statistiques`, ajouter indépendamment une comparaison automatique entre la période filtrée et les mêmes dates calendaires de l'année précédente. Appliquer cette lecture aux quatre blocs `Clients`, `Comptes ouverts et comptes bloques`, `Credits` et `Transactions`; conserver les montants par devise et afficher la couverture `Complete`, `Partielle` ou `Non calculable`.
- Construire `Projections` comme un pilote prudent de court terme, fondé exclusivement sur les historiques Solution Numérique. Proposer une date de référence, un horizon de 7, 15, 30, 60 ou 90 jours, une restitution par jour/semaine/mois et un intervalle de 80 % ou 95 %. Prévoir les créations de clients et de comptes, les nouveaux crédits, les remboursements, les opérations et les volumes; ne jamais mélanger CDF et USD.
- Employer un modèle simple, explicable et testable : combiner le niveau récent avec les derniers jours de semaine comparables, borner la tendance et effectuer un test rétrospectif sur les derniers jours connus. Afficher `MAE — Mean Absolute Error` traduit par `erreur absolue moyenne`, `WAPE — Weighted Absolute Percentage Error` traduit par `erreur absolue pondérée en pourcentage`, ainsi qu'une lecture `Bonne`, `Acceptable` ou `Prudence`; ne jamais présenter une prévision comme une certitude.
- Afficher dans `Projections` un mini-cours destiné aux lecteurs non spécialistes : définition simple du machine learning, rôle de l'historique, différence entre prévision et certitude, interprétation de l'intervalle, de la MAE et du WAPE, importance du jugement du gestionnaire. Tout acronyme anglais visible doit être développé en anglais, traduit en français et expliqué en langage courant.
- Ajouter une infobulle au sélecteur `Intervalle` : expliquer qu'il s'agit de la fourchette d'incertitude entre une borne basse et une borne haute, que 80 % produit une fourchette plus resserrée, que 95 % est plus large et plus prudente, et qu'aucun niveau ne constitue une garantie.
- Ajouter une infobulle à `Horizon` pour expliquer le nombre de jours prévus après la date de référence et l'augmentation habituelle de l'incertitude avec la durée. Ajouter une infobulle à `Affichage` pour préciser qu'il regroupe les mêmes résultats par jour, semaine ou mois sans modifier le modèle ni l'horizon; sommer les volumes et présenter les clients actifs en moyenne.
- Traiter l'échéancier DAT séparément : les dates contractuelles, le capital et l'intérêt estimé constituent un calcul déterministe, pas du machine learning. Un seul instantané `Savings Account` ou `Loans Account` ne permet pas de prévoir les soldes futurs, l'encours futur, le défaut individuel ni un chiffre d'affaires certifié.
- Avertir lorsque l'historique est insuffisant, lorsque la dernière journée est partielle ou lorsque Transactions [Turbo] contient exactement 100 000 lignes et peut correspondre à un plafond d'export. G2 et Perfect ne doivent influencer ni l'apprentissage, ni les montants, ni les intervalles.
- Présenter la comparaison annuelle avec des cartes et des histogrammes groupés période analysée / même période N-1. Ne jamais placer sur le même axe des nombres et des montants, ni des montants CDF et USD. Les graphiques doivent reprendre l'option commune `Afficher annotations (valeurs)` et utiliser un fond blanc lors du téléchargement.
- Qualifier cette lecture de `tendance annuelle` ou `tendance saisonnière indicative`. Une seule année N-1 ne suffit pas à définir une normalité statistique; privilégier au moins trois périodes annuelles comparables pour construire une plage saisonnière habituelle. Ne jamais présenter une rentrée scolaire, un événement social, politique ou environnemental comme cause démontrée par Turbo : le mentionner uniquement comme hypothèse de contexte à valider.
- Appliquer cette lecture dans `Finance et comptabilité`, `Épargnes`, `Solution Numérique / M-Pesa`, `Crédits` et les quatre blocs de `Statistiques`. Comparer au minimum les clients actifs et nouveaux clients, les nouveaux comptes ouverts et DAT, les nouveaux credits et remboursements, ainsi que les operations, volumes et chiffre d'affaires observe.
- Comparer les comptes et les credits selon leur creation ou activation. `Savings Account` et `Loans Account` sont des instantanes : ne jamais presenter la variation de leur solde ou de leur encours actuel comme une evolution hebdomadaire historique sans deux instantanes dates. Dans le rapport Statistiques, les encours épargne ouverte, épargne bloquée/DAT et crédits comparés entre deux fenêtres correspondent au solde instantané des comptes ou crédits créés/activés dans chaque fenêtre, toujours par devise. Ajouter aussi les épargnes sans crédit actif, les DAT arrivant à échéance en volume et en encours, ainsi que le taux de conversion DAT en crédit : clients avec DAT positif et crédit actif rapportés aux clients avec DAT positif de la même devise.
- Dans `Statistiques`, ajouter la régularité des dépôts sur compte ouvert depuis les événements consolidés Transactions [Solution Numérique]. Retenir uniquement les dépôts `NORMAL SAVINGS` / `Epargne depot`; ne pas compter les écritures miroir ni les DAT comme dépôts réguliers du compte ouvert. Le score est `jours avec dépôt / nombre de jours de la période * 100`, par client et par devise. Classer les clients en `Très régulier` (>= 70 %), `Régulier` (>= 40 %), `Occasionnel` (>= 10 %) et `Faible activité` (< 10 %). Cette lecture décrit une discipline d'épargne observée et ne constitue jamais une décision automatique d'octroi de crédit.
- Dans le rapport Word `Statistiques`, l'epargne, les DAT et le credit doivent avoir deux lectures distinctes : `position arretee a la date de fin` et `lecture de periode`. La position arretee a la date de fin conserve les comptes/credits crees ou actives avant ou a cette date, en excluant les comptes clotures avant cette date lorsque la date de cloture est disponible. La lecture de periode montre seulement les comptes ou credits crees/actives entre `Date de debut` et `Date de fin`, avec leur solde ou encours instantane observe. Ne jamais confondre cette lecture de periode avec un encours moyen historique.
- Dans l'onglet `Epargnes`, les blocs de portefeuille et DAT doivent utiliser la meme `position arretee a la date de fin` que le rapport `Statistiques` lorsque l'utilisateur choisit une periode. Ne pas afficher le stock complet du fichier charge comme s'il etait l'encours de la date filtree. Le stock complet reste une information de source/importation, pas un chiffre de reporting periode.
- Un `DAT positif` signifie toujours `balance > 0`, strictement superieur a zero. Ne jamais classer un DAT a solde nul comme DAT positif, ni dans les clients DAT, ni dans le taux de conversion DAT en credit, ni dans les DAT arrivant a echeance.
- Conserver les nombres consolidables au niveau global, mais calculer tous les montants comparatifs par devise. Ne jamais produire une evolution monetaire CDF + USD. Lorsque la période précédente vaut zéro, afficher `Nouvelle activité` au lieu d'un pourcentage infini; lorsque la couverture source est insuffisante, afficher `Référence indisponible` ou `Couverture partielle`.
- Proposer dans `Statistiques` un export Word operationnel du rapport statistiques avec logo, criteres, comparaison avec la periode precedente, puis trois blocs lisibles par la Direction : `Clients`, `Epargnes et DAT` et `Credits`. Supprimer du Word les comparaisons annuelles, le bloc `Transactions`, le chiffre d'affaires, les volumes transactionnels, la qualite G2 et les annexes larges; ces analyses peuvent rester a l'ecran ou dans des exports Excel specialises. Le Word doit prioriser les stocks decisionnels : `Savings Account` pour les comptes ouverts, comptes bloques/DAT, encours epargne, DAT arrivant a echeance et epargne/DAT sans credit actif; `Loans Account` pour les comptes credit, encours credit et nouveaux credits de la periode. Les libelles doivent etre operationnels et non techniques : `Clients recenses dans Customers`, `Clients connus a la date d'arrete`, `Clients actifs sur la periode`, `Nouveaux clients crees sur la periode`, `Comptes bloques / DAT a la date d'arrete`, `Encours credits a la date d'arrete`, `Taux de conversion DAT en credit`. Les montants restent toujours separes par devise; les comptes et clients peuvent etre consolides globalement.
- Dans le Word et l'Excel `Statistiques`, chaque tableau operationnel doit placer une colonne `Explication` immediatement apres `Indicateur`. Cette colonne explique en une phrase ce que mesure l'indicateur afin que la Direction et les equipes operationnelles puissent lire le rapport sans connaitre les noms techniques des fichiers.
- Dans `Statistiques`, l'export Excel doit suivre la meme logique operationnelle que le Word : produire en tete les feuilles `Stats_Comparaison`, `Stats_Clients`, `Stats_Epargne_Oper` et `Stats_Credits_Oper`, puis seulement les feuilles sources utiles (`Stats_Clients_Source`, `Stats_Epargne_Sans_Credit`, `Stats_DAT_Echeance`, `Stats_DAT_Credit`). Ne pas reintegrer dans cet Excel operationnel les feuilles de transactions, chiffre d'affaires, volumes transactionnels, comparaison annuelle ou qualite G2. Les libelles des feuilles operationnelles doivent etre lisibles par la Direction, separes par devise pour les montants, et exploitables sans retraitement technique.
- Harmoniser le vocabulaire de `Statistiques` avec la logique microfinance classique : dans la Solution Numerique, un `compte client` correspond au numero de telephone normalise. Ce compte client peut porter plusieurs produits financiers : produit d'epargne ouvert, produit DAT / compte bloque et produit credit. Les rapports Word/Excel et les KPI visibles doivent donc privilegier les formulations `Comptes clients ...` pour la base client, puis `Produits ...` pour l'epargne, le DAT et le credit. Exemple : `Comptes clients actifs`, `Comptes clients avec produit DAT positif`, `Produits DAT / comptes bloques a la date d'arrete`, `Produits credit a la date d'arrete`. Eviter de formuler comme si un client ne pouvait avoir qu'un seul produit.
- Exiger Streamlit 1.59 ou une version ultérieure compatible avec les fragments écrivant dans les conteneurs `st.tabs` créés à l'extérieur du fragment.

## Norme visuelle commune des onglets

- Ajouter une infobulle `help` à tout contrôle dont le sens, la source, l'unité, la valeur par défaut ou l'effet sur les calculs n'est pas immédiatement évident pour un utilisateur non spécialiste. Couvrir au minimum les dates et heures, fréquences, horizons, intervalles, taux, seuils, devises, statuts de contrôle, recherches par identifiant, soldes d'ouverture, classements et sélections de graphiques.
- Ajouter aussi une aide contextuelle aux titres de blocs analytiques importants, pas seulement aux widgets : expliquer pourquoi le bloc existe, quelle source l'alimente, comment lire le résultat et quelle décision il prépare. Cette aide doit rester compacte afin de ne pas allonger inutilement les pages.
- Tous les contrôles Streamlit de date (`st.date_input`) de Solution M-PESA doivent afficher le format français `jour/mois/année` avec `format="DD/MM/YYYY"`, notamment `Date de début`, `Date de fin`, `Date de référence`, `Date de situation` et toute date d'analyse.
- Rédiger chaque infobulle en français courant. Expliquer d'abord ce que le contrôle signifie, puis ce qu'il modifie et, lorsque cela évite une confusion, ce qu'il ne modifie pas. Préciser le comportement d'une sélection vide, la séparation des devises, le caractère inclusif des dates, les valeurs par défaut et la différence entre une alerte de contrôle et une erreur démontrée.
- Éviter les noms techniques de colonnes dans une infobulle destinée au grand public. Lorsqu'un terme technique ou un acronyme est nécessaire, donner son appellation complète, sa traduction française et une explication concrète. Ne pas ajouter d'infobulle aux actions dont le sens est déjà évident, sauf si leur portée ou leur effet est irréversible ou ambigu.
- Appliquer cette norme à tous les niveaux de navigation de Solution M-PESA : sous-onglets principaux, sous-sous-onglets de `Finance et comptabilité`, `Perfect Client`, `Solution Numérique / M-Pesa` et tout futur bloc `st.tabs`.
- Lorsqu'un tableau d'anomalies, d'alertes, d'écarts ou d'éléments à vérifier n'est pas vide, afficher immédiatement au-dessus une bannière rouge native `st.error` indiquant le nombre de signaux et l'action attendue. Si le tableau est vide, conserver une confirmation verte `st.success`.
- Conserver le tableau lui-même sobre et sans coloration rouge; la bannière porte seule la surbrillance afin d'attirer l'attention sans réduire la lisibilité des données.
- Dans `Importation et contrôle`, construire `Anomalies Transactions [Turbo]` avec une colonne `raison_anomalie` visible en première position et épinglée. Détailler tous les contrôles transactionnels au statut d'alerte : identifiants manquants, date invalide, mouvement nul, `dr` et `cr` simultanément positifs, solde négatif, devise ou type de compte manquant/inconnu, doublon exact et groupe répété. Appliquer les mêmes filtres `statut` et `controle` à la synthèse et à la liste. Faire correspondre la valeur de chaque contrôle au nombre de lignes Turbo détaillables; pour les répétitions, compter les lignes et conserver le nombre de groupes dans `detail`. Lorsque `controle` est filtré, limiter `raison_anomalie` aux seuls motifs sélectionnés; sans filtre, concaténer les motifs lorsqu'une ligne cumule plusieurs anomalies.
- Conserver une barre d'onglets sobre et professionnelle, avec des espacements réguliers entre les libellés.
- Afficher l'onglet actif en bleu, avec des coins arrondis et un soulignement rouge.
- Appliquer un survol discret et rendre la navigation au clavier clairement visible avec `:focus-visible`.
- Permettre le défilement horizontal des onglets sur les petits écrans, sans retour à la ligne.
- Encapsuler chaque barre `st.tabs` dans un `st.container(key=...)` doté d'une clé CSS unique. Ne jamais créer directement un sous-sous-onglet avec `st.tabs` hors d'un conteneur ciblé.
- Appeler `inject_professional_tabs_css(container_key=...)`, puis `format_professional_tab_labels(...)`, afin de réutiliser la norme commune de `credit_app/ui.py`. Ne pas dupliquer ce CSS dans `solution_mpesa.py`.
- Choisir des clés stables et propres au contexte, par exemple `mpesa_solution_tabs`, `mpesa_finance_turbo_inner_tabs`, `mpesa_perfect_client_cohort_tabs` et `mpesa_g2_temporal_detail_tabs`, afin d'éviter les collisions et les débordements de style entre niveaux.
- Préserver le mode de calcul défini dans la section précédente : l'habillage visuel doit rester indépendant de la logique métier, mais la performance prime sur Streamlit Cloud. Les onglets peuvent être rendus conditionnellement si les sources communes, les règles métier et les caches garantissent les mêmes résultats à l'ouverture du volet.

## Exports

- Générer uniquement les feuilles Excel demandées par le contexte; ne jamais ajouter automatiquement toutes les feuilles vides du module.
- Tous les documents Word de Solution M-PESA utilisent des marges gauche et droite de 2 cm. Préserver l'A4 portrait lorsqu'il est déjà contractuel, puis compacter les colonnes et polices au lieu de réduire ces marges.
- Pour G2/DAT, conserver la synthèse, les comptages, le détail, les analyses temporelles et la fidélisation. Nommer les feuilles de contrôle `Statuts_G2`, `Anomalies_G2`, `G2_DAT` en mode G2 et `Statuts_Turbo`, `Anomalies_Turbo`, `Turbo_DAT` en mode Turbo seul.
- Garder le Word modifiable et ajouter en annexe le tableau unique `Transactions`, dans le même ordre que l'écran. Dans `G2/DAT`, tous les exports imprimables du bloc `7. Export du rapport [G2/Turbo]` doivent être en A4 portrait, annexe `Transactions` comprise; compacter les largeurs et la police sans supprimer les colonnes contractuelles. Reprendre l'identité visuelle de l'Extrait client : logo officiel Bisou Bisou, bandeau de critères, titres bleu institutionnel, tableaux uniformes et pied de page daté. Dans le bandeau, ne pas regrouper les bornes sous `Période` : afficher `Date du :` avec la date et l'heure de début, puis `Au :` avec la date et l'heure de fin. Régler aussi l'orientation d'impression des feuilles Excel sur portrait.
- Transmettre `rapport_journalier_pivot` au Word même lorsqu'il est exclu de l'Excel compact; reconstruire la synthèse par devise depuis le détail si le pivot manque.
- Répéter les en-têtes Word sur plusieurs pages et conserver toutes les lignes `Completed` du périmètre filtré; garder les autres statuts dans l'Excel de contrôle.
- Calculer la ligne `Activite` de la synthèse exécutive Word directement depuis le détail `Completed` filtré par date, heure et sens; ne jamais la reprendre du dernier mois de fidélisation.
- Vérifier qu'un export client reprend les filtres de l'extrait sans perdre les feuilles contextuelles du client.
- Pour le relevé Word, vérifier les trois sorties CDF, USD et ALL, l'en-tête `Devise` et les synthèses multidevises séparées.
- Proposer le relevé en CDF, USD et ALL avec le même périmètre filtré et les mêmes règles de séparation des devises pour Word et PDF. Générer le PDF nativement avec ReportLab afin de fonctionner sur Streamlit en ligne sans navigateur système.
- Générer le Word et le PDF de l'Extrait client en A4 portrait. Garder un seul tableau `DAT en cours` et un seul tableau `Remboursements observés`, avec des largeurs adaptées au portrait; garder le détail transactionnel compact et autoriser le renvoi à la ligne de la description. Les exports G2/DAT suivent désormais la même orientation portrait sur toutes leurs pages.
- Vérifier que les Word et PDF ne contiennent ni `Situation financière actuelle du client` ni solde du portefeuille M-PESA comme bloc séparé. Commencer par le tableau unique `Synthèse financière par devise`, sans bloc séparé `Situation de l'épargne`, sans mention `point de vue Bisou Bisou`, sans phrase explicative; conserver l'ouverture et la clôture dans cette synthèse lorsqu'elles sont observables depuis Turbo.
- Uniformiser la mise en forme de tous les tableaux de l'Extrait client. Le premier tableau `Synthèse financière par devise` doit utiliser la même grille, la même typographie, les mêmes espacements et le même style d'en-tête que les autres tableaux du document Word ou PDF; ne pas le mettre en forme comme un bloc spécial.
- Utiliser `Référence Turbo`, `Entrées`, `Sorties` et `Solde` dans le détail Word/PDF. Le détail Word/PDF doit reprendre les six opérations bancaires prévues : dépôt, retrait, retrait de DAT avant échéance, retour du montant principal du DAT, entrée des intérêts sur DAT et remboursement d'un crédit depuis le compte ouvert. Le prêt net, le dépôt DAT initial, le solde d'ouverture et la clôture restent dans leurs blocs dédiés et ne doivent pas être injectés artificiellement dans ce solde.
- Proposer un export minimal de l'Extrait client, séparé du format global, en Word et en PDF. Ce format minimal garde l'en-tête, les critères, `Synthèse financière par devise` avec ouverture/clôture, puis seulement le bloc `Détail des transactions`; il ne développe pas `Éléments couverts par l'extrait client`, `DAT en cours`, `Remboursements observés`, retours DAT ou intérêts DAT crédités.
- Proposer les exports du relevé en Word et PDF, chacun en format `global` et `minimal`. Le format global garde la synthèse financière par devise, les DAT en cours pertinents, les remboursements observés et le détail des transactions. Le format minimal garde l'en-tête, les critères, la synthèse financière par devise et le détail des transactions. Aucun export ne doit afficher `Cumul net des flux`, `vue client`, `vue Bisou Bisou`, `point de vue Bisou Bisou` ou `Synthèse de la position client` dans le document.
- Dans tous les exports de l'Extrait client qui affichent un bloc `DAT en cours`, inclure obligatoirement la colonne `Jours restants`, y compris dans les exports globaux et dans la feuille Excel `DAT_En_Cours`.
- Ajouter `Taux annuel DAT` aux critères du Word et du PDF. Dans `DAT en cours`, conserver exactement `DAT`, `Souscription`, `Échéance`, `Jours restants`, `Devise`, `Capital bloqué`, `Situation` et `Capital + intérêt estimé`.
- Dans `Remboursements observés`, conserver exactement `Date`, `Référence`, `Devise`, `Montant payé`, `Intérêts`, `Origine du paiement` et `Pénalités`. Ne pas afficher le principal, le mode observé, les décaissements ni une section crédit.
- Dans l'Excel client, utiliser `DAT_En_Cours`, `Remboursements_Turbo`, `Elements_Extrait_Turbo` et `Interets_DAT_Credites`; ne pas exporter l'ancienne feuille `Interets_DAT_Echus`, `Credit_Client_Turbo`, `Positions_Turbo` ou `Credits`.
- Générer les exports de balance Word/PDF à la demande dans `Balances et journaux`, avec des boutons compacts et sans recalculer les autres volets. Les deux exports de balance observée doivent être en A4 portrait, avec des colonnes compactes adaptées à une lecture Direction.
- Dans `Finance Turbo > Balances et journaux`, remplacer l'ancien export de balance par date par un `Suivi des dépôts et retraits par client [Turbo]` inspiré de l'état Vodacom : une ligne `Depot` et une ligne `Retrait` par client et devise, une colonne par jour de la période filtrée, puis `Total`, `Solde` et `Score`. Construire ce suivi uniquement depuis `NORMAL SAVINGS` (`Epargne depot` et `Retrait Vers M-Pesa`); utiliser G2 seulement pour enrichir le nom du client lorsque G2 est téléversé. `Solde` correspond au solde du compte ouvert lorsqu'il est disponible; sinon utiliser le solde net dépôt/retrait de la période. Ajouter un second suivi Excel distinct `Suivi des dépôts et retraits de DAT par client`, construit uniquement depuis `FIXED SAVINGS` (`Depot Bloque` et `Retrait Compte Bloque`), avec le solde DAT lorsqu'il est disponible. Le score correspond au nombre d'opérations observées rapporté au nombre de jours de la période. L'export opérationnel de ces suivis est Excel afin de permettre le tri, le filtrage et le retraitement; ne pas privilégier le PDF pour ces tableaux larges.
- Intégrer le logo officiel `skills/logo Bisou Bisou.PNG` dans les exports Word et PDF de l'Extrait client; conserver un libellé texte de repli si l'image est absente ou illisible.
- Nommer le Word Turbo seul `extrait_compte_<customer_id>_<telephone>_<devise>_<debut>_<fin>.docx`. Si G2 est chargé, insérer le nom G2 entre l'identifiant et le téléphone. Conserver les espaces du nom, supprimer uniquement les caractères interdits des noms de fichiers et ne jamais utiliser G2 pour recalculer les montants.
- Dans le relevé Word, retirer tous les suffixes `[Turbo]`. Le détail doit rester centré sur le compte ouvert : les prêts nets et les DAT sont présentés dans la synthèse ou les blocs dédiés, pas comme lignes du compte ouvert. Afficher le solde d'ouverture et la clôture uniquement dans `Synthèse financière par devise`.
- Dans le format minimal, ne pas inclure les tableaux `Synthese du comportement observe`, `Positions observees et rapprochement des soldes` et `Jalons du parcours financier`. Conserver obligatoirement `Detail des transactions`. Le pied de page porte `Solution Bisou Bisou Digital`.
- Dans le titre du relevé Word, omettre entièrement le segment du nom lorsque celui-ci est vide, `Non disponible` ou `Nom non disponible`; produire alors `Extrait de compte - <telephone> - <devise>` sans séparateur vide.
- Exporter les trois populations `Clients_Perfect` dans `Clients_Perfect_G2`, `Clients_Perfect_Turbo` et `Clients_Perfect_Turbo_G2`.
- Dans l'export de pilotage du volet `Finance Turbo > Export`, n'inclure que les synthèses et listes d'action Turbo demandées : flux, remboursements, nouveaux crédits, encours/PAR, épargne, DAT, concentrations, alertes, contrôles et définitions. Ne produire aucune feuille de montant G2.
- Prioriser les analyses en ligne : les KPI, tableaux filtres, alertes, commentaires et graphiques affiches dans Streamlit constituent la restitution principale. Les exports Excel sont des supports secondaires de retraitement, d'audit ou de partage; ils ne doivent jamais etre generes automatiquement au chargement d'un onglet.
- Generer l'Excel des cockpits uniquement sur demande avec un bouton explicite `Preparer`, puis afficher le bouton `Telecharger` seulement lorsque le classeur est construit. Appliquer cette regle aux exports volumineux des onglets `Clients`, `Epargnes`, `Credits`, `Solution Numerique / M-Pesa`, `Finance et comptabilite`, `G2/DAT`, `Perfect Client` et `Statistiques`. Limiter les feuilles aux syntheses, details et listes d'action utiles afin de ne pas alourdir le premier chargement ni les reruns Streamlit.
- Lorsqu'un cockpit Excel `Clients`, `Credits` ou `Epargnes` est partage a la Direction, limiter le classeur aux feuilles importantes et indiquer les feuilles prioritaires a lire. Ne pas exporter par defaut les grands details techniques comme `Clients_360`, `Credit_Detail` ou `Epargne_Detail`; ils restent des vues de diagnostic en ligne ou des exports specifiques si le besoin est explicite. Pour `Clients`, recommander d'abord `Clients_KPI`, `Clients_Acquisition`, `Nouveaux_Clients_Actifs`, `Clients_Actifs`, `Clients_Sans_Mouvement`, `Clients_Multi_Produits`, `Clients_Tranches`, `DAT_Sans_Credit` et `Clients_Qualite`. Pour `Credits`, recommander `Credit_Vue_Ensemble`, `Credit_Encours_A_Date`, `Credit_Risque_Synthese`, `Credit_Echeances`, `Liste_Prets_Echus`, `Liste_Prets_PAR30`, `Liste_Prets_Penalites`, `Liste_Prets_Defaulted`, `Credit_Top_Clients`, `Credit_Tranches_Clients` et `Credit_Epargne_Clients_360`. Pour `Epargnes`, recommander `Epargne_Vue_Ensemble`, `Epargne_Encours_A_Date`, `Epargne_DAT`, `Epargne_Echeances_DAT`, `Epargne_Top_Clients`, `Epargne_Tranches`, `Epargne_DAT_Sans_Credit`, `Epargne_Forte_Sans_Credit`, `Epargne_Qualite` et `Epargne_Qualite_A_Revoir`. Presenter les feuilles de synthese a la Direction, les listes detaillees aux operations/commercial, et les feuilles qualite/diagnostic au controle ou a la data.
- Dans les cockpits Excel `Clients`, `Credits` et `Epargnes`, colorer en rouge l'onglet Excel des feuilles prioritaires de lecture afin que la Direction les repere immediatement. Cette couleur signale une priorite de consultation, pas une anomalie; les tableaux d'anomalies gardent leurs bannieres rouges a l'ecran.
- Dans ces feuilles prioritaires, supprimer les colonnes techniques inutiles a la lecture operationnelle : fichiers sources, cles internes, colonnes brutes, ordres d'import, colonnes de debug, traces techniques et identifiants intermediaires. Conserver les colonnes metier utiles a l'action : client, telephone, devise, produit, statut, date, montant, solde, encours, echeance, risque, tranche, score, ratio et commentaire. Les feuilles non prioritaires peuvent garder plus de tracabilite pour le controle et la data.
- Dans les cockpits Excel `Clients`, `Credits` et `Epargnes`, toute feuille operationnelle prioritaire qui affiche un nom client doit aussi afficher une colonne `numero_client` avant ou a cote du nom, renseignee de preference depuis le telephone (`numero_telephone`, `telephone`, `msisdn`, `msisdn1`, etc.), avec repli sur l'identifiant client si aucun telephone n'est disponible. L'objectif est de permettre aux equipes d'appeler ou retrouver rapidement le client depuis l'export partage.
- Pour l'export comptable distinct de `Finance Turbo > Export`, exporter uniquement les feuilles demandées parmi `Compta_Synthese_Turbo`, `Balance_Clients_Turbo`, `Positions_Clients_Turbo`, `Balance_Comptes_Turbo`, `Journal_Operations_Turbo`, `Journal_Ecritures_Turbo`, `Controles_Operations_Turbo`, `Controles_Soldes_Turbo`, `Flux_MPESA_Turbo`, `Produits_Financiers_Turbo`, `Positions_Portefeuille_Turbo` et `Controle_G2_Turbo`.

## Architecture à respecter

- Contrats : `credit_app/data_schema.py`
- Calculs, rapprochements et exports : `credit_app/services/mpesa_analysis.py`
- Interface : `credit_app/tabs/solution_mpesa.py`
- Tests : `tests/test_mpesa_analysis.py`

Placer les règles déterministes dans le service, le rendu dans l'onglet Streamlit et chaque nouveau cas métier dans les tests.

## Validation

Utiliser le cahier de tests local `Scénario Turbo et G2 client TEST.docx`, non versionné, comme référence fonctionnelle évolutive pour un parcours client anonymisé. Lire ses paramètres, étapes, références Turbo/G2 et résultats financiers attendus avant de corriger une règle métier; transformer chaque nouvel écart démontré en test automatisé. Conserver Turbo comme source des montants et G2 comme preuve de contrôle. Ne jamais transformer les valeurs d'un scénario particulier en seuils métier permanents.

Exécuter au minimum avec l'environnement Python du projet :

```powershell
& $PYTHON skills/solution-mpesa/scripts/inspect_mpesa_contracts.py
& $PYTHON -m pytest tests/test_mpesa_analysis.py -q
```

Pour un changement G2/DAT, tester un fichier réel sans l'écrire dans le dépôt et vérifier le nombre de reçus, l'ordre des colonnes, les devises, les totaux et les anomalies. Vérifier également que chaque Excel contient seulement les feuilles prévues. Pour `Perfect_client`, vérifier les trois populations inclusives et leurs trois feuilles Excel avec un export 122 réel.

Pour un changement du volet comptable de `Finance Turbo`, tester la journée de référence du 16 juillet 2026 lorsqu'elle est disponible, vérifier les 12 feuilles comptables, le rapprochement G2 direct, la séparation CDF/USD et la concordance entre synthèse, balances, journaux et contrôles. Une opération non symétrique ou une variation de solde à revoir est un signal de contrôle; ne jamais la qualifier automatiquement d'erreur comptable.

Pour un changement du volet pilotage de `Finance Turbo`, tester également le 16 juillet 2026 : attendre 135 événements consolidés, 48 CDF et 87 USD; vérifier 284 910 CDF et 194,54 USD de remboursements observés, 122 200 CDF et 99 USD de nouveaux crédits décaissés, la séparation stricte des devises et l'absence totale des montants G2. Mesurer la consolidation et le rapprochement crédit-épargne sur les fichiers réels afin de prévenir toute régression de performance.

Pour un changement des crédits dans l'Extrait client, rejouer le scénario `PRET_TEST_001` : vérifier 12 écritures d'octroi, 4 écritures de remboursement séparées, un prêt brut de 5,00 USD, un intérêt de 0,35 USD correspondant à 7 %, un net versé de 4,65 USD dans l'extrait, 5,00 USD dans `Finance Turbo > Nouveaux crédits` et 0,35 USD dans les produits financiers.

Pour un changement DAT ou de mise en page de l'Extrait client, charger aussi le `Savings Account` associé au scénario `243000000000` et rejouer son diagnostic de soldes. Attendre deux comptes courants à solde nul et le DAT USD `DAT_TEST_001` à 10,00 USD, souscrit le 18/07/2026 et échéant le 18/08/2026. Vérifier les valeurs attendues dans les structures de données : le DAT reste `En cours`, `Synthèse financière par devise` affiche le solde d'ouverture saisi, 15,00 USD d'entrées externes, 4,65 USD de sorties externes, 10,35 USD de flux net externe, la clôture égale à l'ouverture + 10,35 USD, 0,00 USD en compte ouvert et 10,00 USD en compte bloqué. Le remboursement interne de 5,00 USD reste dans `Remboursements observés` avec 0,00 USD d'intérêts et ne doit jamais augmenter les entrées externes. Le `Détail des transactions` doit afficher uniquement les lignes du compte ouvert : `DEPOT_TEST_001` en entrée de 5,00 USD et `RETRAIT_TEST_001` en sortie de 5,00 USD; il doit exclure `DAT_MOUVEMENT_TEST_001` (DAT) et `PRET_TEST_001` (prêt). Le PDF minimal doit s'arrêter au bloc `Détail des transactions` après la synthèse.
